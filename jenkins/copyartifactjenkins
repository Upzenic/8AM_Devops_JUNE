pipeline {
    agent any

    stages {
        stage('Copy Artifact') {
            steps {
                copyArtifacts projectName: 'job1'

                bat 'type output.txt'
            }
        }
    }
}
