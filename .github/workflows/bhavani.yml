name: My Workflow

on:
  workflow_dispatch: 
  push:
    branches:
      - bhavani

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: echo "Build successful!"                 
           
